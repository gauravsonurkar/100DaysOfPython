# Creating the tuples using the ()
# t = (1,2,4,5)
#print the values of the tuples
# print(t[0])
#we can not update the values in the tuples
# t[0] = 34 # tuple is an immutable dataType
# print(t) 

a = () # Empty tuples 
print(a)

a1 =  (1 ) # Wrong way to declare a tuple with single element
a1 = (1,) #Tuple with single elements
print(type(a1))